{
  "name": "monopoly-machine",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "cd front && yarn build && cd ../back && yarn start",
    "dev": "cd front && yarn build-dev && cd ../back && yarn dev",
    "back": "cd back && yarn dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}